import 'package:flutter/material.dart';

/// ScanServe brand palette.
/// Warm orange/coral accent on neutral, clean backgrounds.
class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFFFF6B4A); // warm coral/orange
  static const Color primaryDark = Color(0xFFE85A3B);
  static const Color primaryLight = Color(0xFFFFE4DB);

  // Neutrals
  static const Color background = Color(0xFFFAFAF9);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE9E7E4);

  static const Color textPrimary = Color(0xFF1F1B18);
  static const Color textSecondary = Color(0xFF6B6560);
  static const Color textMuted = Color(0xFF9C9691);

  // Feedback
  static const Color success = Color(0xFF2E9E5B);
  static const Color error = Color(0xFFD64545);
}
